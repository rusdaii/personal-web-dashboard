import LoginForm from '@/components/parts/LoginForm';

const Login = () => {
  return (
    <div className="h-screen grid place-content-center fixed inset-0">
      <LoginForm />
    </div>
  );
};

export default Login;
